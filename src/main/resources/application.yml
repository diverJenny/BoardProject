spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/okBoard?serverTimezone=UTC&allowPublicKeyRetrieval=true
    driver-class-name: org.mariadb.jdbc.Driver
    username: root
    password: qwer1234
  # ???? spring.io? doc?? ?????.
  # JPA ??
  jpa:
    database-platform: org.hibernate.dialect.MariaDBDialect
    open-in-view: false
    show-sql: true
    hibernate:
      format_sql: true
      ddl-auto: update
server:
  port: 3000